(setglobal Example2!
  (let
    (r/81 =
       (function param/82[int]
         (catch
           (if (!= param/82 1) (if (!= param/82 2) (exit 1) (observe 2))
             (observe 1))
          with (1) (observe 0))))
    (makeblock 0 r/81)))
